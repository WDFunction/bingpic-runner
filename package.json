{
  "scripts": {
    "build": "ncc build index.js -m",
    "deploy":"yarn build && fun deploy -y"
  },
  "dependencies": {
    "ali-oss": "^6.13.2",
    "axios": "^0.21.1",
    "dotenv": "^8.2.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.27.0"
  }
}
